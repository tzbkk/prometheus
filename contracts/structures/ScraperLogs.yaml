SchemaVersion: 1
Type: structure
Name: ScraperLogs
Description: >-
  GET /logs 响应（scraper）。日志尾行数组（tail 查询参数不入边）。
Location: urn:prometheus:type:ScraperLogs
Definition:
  Form: record
  Table:
    - Field: lines
      Type: array<string>
      Required: true
      Description: 日志尾行（裸行数组）。
